import { EnumToListPipe } from './enum-to-list.pipe';

describe('EnumToListPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumToListPipe();
    expect(pipe).toBeTruthy();
  });
});
